# VayMah

VayMah is a modern Next.js storefront built with TypeScript, Tailwind CSS, Three.js, React Three Fiber, drei, and GSAP.

## Scripts

```bash
npm install
npm run dev
```

Open `http://localhost:3000` after the dev server starts.
